const ALL = 'all';
const JOIN = 'join';
const CONNECTION = 'connect';
const DISCONNECT = 'disconnect';
const CHAT = 'chat';
const LEAVE = 'leave';

module.exports = {
  CONNECTION,
  DISCONNECT,
  ALL,
  JOIN,
  CHAT,
  LEAVE,
};
